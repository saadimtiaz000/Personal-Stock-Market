services:
  - type: web
    name: personal-stock-market
    runtime: node
    plan: free
    buildCommand: npm install && npm run check
    startCommand: npm run api
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
